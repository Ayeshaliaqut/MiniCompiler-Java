MiniCompiler — Java

This repository contains a small, end-to-end syntax-analysis demo compiler (lexer + recursive-descent parser + AST) written in Java and packaged as a Maven project so it's ready to push to GitHub.

It includes:

Build files (Maven pom.xml)

Source code under src/main/java in package com.example.minicompiler

README.md with build/run instructions and example

.gitignore and LICENSE (MIT)
